# ----- OS / Editor -----
.DS_Store
Thumbs.db

.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# ----- Rust / Cargo -----
/**/target/
**/*.rs.bk
Cargo.lock

# If you publish a library crate, keep Cargo.lock; for apps it can go either way.
# For a game repo, ignoring is fine; if you prefer to commit it, remove the line above.

# ----- wasm-pack output -----
/engine/pkg/

# ----- Node / Vite -----
/web/node_modules/
/web/dist/
/web/.vite/
/web/.env
/web/.env.*.local

# wasm-pack output (now inside web)
 /web/src/wasm/
/web/src/wasm_version.ts

# ----- Logs -----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# ----- Devcontainer local caches (usually not present, but safe) -----
.devcontainer/.cache/
